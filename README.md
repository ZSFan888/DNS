# DNS SaaS MVP

## Goal
A multi-tenant subdomain distribution platform with real DNS record control.

## First milestone
- Manage root domains.
- Register subdomains.
- CRUD DNS records.
- Audit logs.

## API
- GET /api/root-domains
- POST /api/root-domains
